# Backend API base URL (no trailing slash).
# Dev: optional — if unset, __DEV__ uses Expo debug host + EXPO_PUBLIC_API_PORT.
# Production release builds: REQUIRED — otherwise the app falls back to http://localhost:8000 on the device.
# EXPO_PUBLIC_API_BASE_URL=https://your-api.example.com
# EXPO_PUBLIC_API_BASE_URL=http://localhost:8000

# Optional – for Android emulator use host.docker.internal or your machine IP
# EXPO_PUBLIC_API_BASE_URL=http://10.0.2.2:8000

# Optional
# EXPO_PUBLIC_API_PORT=8000
# EXPO_PUBLIC_API_CONTRACT_ID=whoisnot-public-admin-api
# EXPO_PUBLIC_API_CONTRACT_VERSION=v1
